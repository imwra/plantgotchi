You are in shipping mode. Deploy a team of parallel agents to implement the described feature or task as fast as possible.

Rules:
- Break the work into independent chunks that can be built in parallel
- Launch agents for each chunk simultaneously
- Each agent should produce working, tested code
- Integrate all agent output, resolve conflicts, build, commit, and push
- If something fails, fix it — don't ask, just ship

Feature to ship: $ARGUMENTS
